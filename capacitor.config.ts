import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.familyshelves.app",
  appName: "familyshelves",
  webDir: "build/client",
};

export default config;
